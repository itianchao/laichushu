<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/auditing" />
    <stroke
        android:width="0.6dp"
        android:color="@color/Grey" />
    <corners android:radius="15dp" />
    <size
        android:width="120dp"
        android:height="44dp" />
</shape>